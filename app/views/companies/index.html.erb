<table class="table table-condensed">

  <% @companies.each do |company| %>
    <tr>
      <td><%= company.symbol %></td>
      <td><%= company.name %></td>
      <td><%= link_to 'link', company %></td>
    </tr>
  <% end %>

</table>